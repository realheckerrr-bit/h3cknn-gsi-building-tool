# ==============================================================================
# default_props.txt - Universal Project Treble GSI System Properties
# ==============================================================================

# Core Project Treble compatibility
ro.treble.enabled=true
# Do not force PHH-only or USB transport properties. Vendor init/USB HALs own
# those settings and differ across devices; overriding them is not a universal
# driver and can break first boot or recovery access.
# Do not force generic heap, SurfaceFlinger, strict-mode, or debug values
# either. These are framework/vendor tuning knobs, not drivers, and incorrect
# values can cause display failures, memory pressure, or early boot problems.
